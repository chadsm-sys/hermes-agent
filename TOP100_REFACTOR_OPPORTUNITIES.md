# Top 100 Refactor Opportunities — hermes-agent

**Audit date:** 2026-07-06 · **Commit:** `93b565b` · Ranked by **(engineering hours saved × risk reduction) ÷ effort**. Each entry: evidence (`file:line`), the move, estimated hours saved over ~12 months, effort (S ≤ 4h, M ≤ 2 days, L > 2 days). Cross-references (TD-xxx) point into `TECH_DEBT_REGISTER.md`. Evidence only — no code was modified.

## Tier 1 — Highest leverage (do these first)

1. **Adopt `TextBatchAggregator` in the 8 platform adapters** — the abstraction already exists with zero importers (`gateway/platforms/helpers.py:81`; its docstring names the offenders). Deletes the `_enqueue_text_event`/`_text_batch_key`/`_flush_text_batch` trio from wecom :582, feishu :3506, weixin :1499, telegram :5869, whatsapp :1003, matrix :3266, discord :5307, simplex :677 (≈526 lines, already drifting). ~12h saved · M · TD-030
2. **Delete the byte-identical `_content_display_text` twin** — defined twice in the same module (`tui_gateway/server.py:3582` and `:3783`); the second silently shadows the first. 0.5h · S · TD-044
3. **Merge the diverged test-file forks** — `tests/test_cli_skin_integration.py` vs `tests/cli/test_cli_skin_integration.py` and `tests/test_cli_file_drop.py` vs `tests/cli/…` each carry tests the other lacks. 3h · S · TD-102
4. **Add `--strict-markers` and resolve the phantom timeouts** — every `pytest.mark.timeout(...)` in the repo is a silent no-op (pytest-timeout is not a dependency; the "global --timeout=30" the comments cite doesn't exist). `pyproject.toml:329-336`, `tests/docker/conftest.py:46`. 2h · S · TD-099/110
5. **One generic `ProviderRegistry[T]` for the six provider registries** — tts/transcription (similarity 0.84), image/video (0.79, self-declared mirror already drifted), web_search/browser (0.86); 954 lines → ~150 + 30/family. Also fixes the strip/lower lookup-normalization bug that makes whitespace-named providers unreachable (`agent/image_gen_registry.py:53` vs `:72`) and restores the `_is_available_safe` filtering video_gen never received. ~16h · M · TD-031/090/096
6. **Dedupe test platform mocks onto the existing conftest canonicals** — `_ensure_discord_mock` ×17 (625 lines; `tests/gateway/conftest.py:86` docstring already mandates reuse), `_ensure_telegram_mock` ×18 (386), `_ensure_slack_mock` ×9 (227, needs a canonical), `_make_runner` ×10. ≈1,240 duplicated lines with observed drift. ~22h · M · TD-101
7. **Fix or delete the unrunnable stress suite** — `tests/stress/conftest.py:32-36` `collect_ignore_glob=["*.py"]` makes `--run-stress` collect nothing; 9 stress files masquerade as coverage. 6h · M · TD-071
8. **Decompose `run_conversation` along its own section banners** — `agent/conversation_loop.py:436`, 3,982 lines, cx≈607, ≥14 banner-marked phases (interrupts :530, message prep :638-813, refusal :1387, error-classification block :2168, stream recovery :4019, fallback :4199…). The hottest path in the product. 80h+ · L · TD-001
9. **Generic OAuth flow engine on the existing `ProviderConfig` registry** — `hermes_cli/auth.py` repeats the login/refresh/status/quarantine skeleton 7× (Nous :5259, Codex :3553, xAI :4197-6907, Qwen :1943, Gemini :2165, Spotify :2252, MiniMax :7394); repo-wide there are ≥6 OAuth implementations, 4 loopback callback servers, 3 PKCE impls (`agent/google_oauth.py:380,740`; `tools/mcp_oauth.py:366,483`; `web_server.py:5146-6250`; `dashboard_auth/base.py:30-115`; `main.py:4008`). ~70h · L · TD-034/035
10. **Table-ize `gateway/config.py` env overrides + fix parse inconsistencies** — `_apply_env_overrides` :1348 handles 198 env literals with `HomeChannel(...)` ×18 and per-platform triples ×13; while there, fix `FOO=on` truthiness differing per platform (:1415 vs :1889 vs `_coerce_bool` :25) and bare `int()` aborting config load for wecom/bluebubbles (:1826, :1882). ~19h · M · TD-015/093/094
11. **Finish the half-done `main()` parser extraction** — 38 `build_*_parser` functions already live in `hermes_cli/subcommands/`; ~820 lines of `add_parser` blocks remain inline in `main()` (fallback :11490, secrets :11524, migrate :11562, whatsapp-cloud :11629, checkpoints :11733, bundles :11767, plugins :11803, curator :11833, computer-use :11864, sessions :11938, completion :12312). Enables deleting the bpo-9338 double-parse workaround (:12399-12420). 16h · M · TD-011
12. **Extract the 7 gateway↔TUI parity config loaders** — `_load_reasoning_config`, `_load_service_tier`, `_load_provider_routing`, `_load_fallback_model`, `_load_show_reasoning`, `_load_busy_input_mode`, `_claim_active_session_slot` maintained as admitted manual copies (`gateway/run.py:3447-3700` vs `tui_gateway/server.py:1709-350`; server :1734 docstring says "Parity with the messaging gateway"); divergence already observed. 4h · S · TD-043
13. **Extract `gemini_common` from the twin Gemini adapters** — 23 parallel same-named functions, similarity up to 1.00 (`gemini_native_adapter.py` vs `gemini_cloudcode_adapter.py`; `_make_stream_chunk` 0.91, `_map_gemini_finish_reason` 0.96, shim classes 0.95-0.97); ~250-300 lines extractable. 20h · M · TD-032
14. **Extract `hermes_cli/updater/` from main.py** — the self-updater is ~4,100 lines (:5807-9925) including the 1,513-line `_cmd_update_impl` :8410; seams: git.py / windows.py / deps.py / backup.py. 40h · L · TD-004
15. **Unify `call_llm`/`async_call_llm` and the retry pair** — `agent/auxiliary_client.py:5047` vs `:5571` (similarity 0.74, ~300 dup lines); `_retry_same_provider_sync`/`_async` :2777/:2835 at 0.97. The clean `asyncio.to_thread` wrapper pattern already exists in-file (:952-965). 19h · M · TD-033
16. **Adopt `browser_registry._resolve`; delete the hand-rolled duplicate** — `agent/browser_registry.py:107-186` is production-dead while `tools/browser_tool.py:489-591` re-implements the policy; also removes the tests-only `_PROVIDER_REGISTRY` shim (:423-467). 6h · M · TD-070
17. **Split `web_server.py` into APIRouter modules** — 172 routes + 4 websockets on one app, zero APIRouter usage, ~18 cleanly-partitioned domains (`hermes_cli/web_server.py`). 40h · L · TD-005
18. **Unify the two session REST APIs** — FastAPI (`web_server.py:2554-6642`) and aiohttp (`gateway/platforms/api_server.py:1362-1497`) both CRUD the same `SessionDB` with different auth. 15h · M · TD-036
19. **Rename `curator_backup.py` → `curator_snapshot.py`** — live snapshot/rollback code (imported at `agent/curator.py:1450`, `hermes_cli/curator.py:375`) named like a stale backup copy; deletion bait. 1h · S · TD-072
20. **Purge dead dependencies** — start with root `package.json:35` `@streamdown/math` (eagerly installed for every `hermes update` user, imported nowhere, deliberately replaced by `apps/desktop/src/lib/katex-memo.ts`); then desktop ×6, web ×6 (`Backdrop.tsx:7` brags it avoids deps package.json ships), bootstrap ×4, Python `tenacity`/`ruamel.yaml`. 8h · S · TD-077

## Tier 2 — Structural decomposition (high value, plan-first)

21. **Stage-split `gateway/run.py:_run_agent`** (2,585 lines; nested 938-line `run_sync` closure :14329; recursive self-call :15939) into setup → threaded core → post-turn phases. 40h · L · TD-003
22. **Extract the disjoint-state clusters out of `GatewayRunner`** — Telegram-topics (~15 methods, :2717-:10973), voice/presence (:2498-:10197), restart/shutdown (:4103-:16120), background watchers (:5410-:16209), agent cache (:12764-:13179), run-generation guard (:3700-:12911). Lowest-risk first cuts at the 14,000-line class. 8-15h each · M · TD-002/004(seams)
23. **Stage-split `_handle_message` and `_handle_message_with_agent`** — verified gate→execute pipeline (not clones), but each is a 1,200-line method (`gateway/run.py:6654`, `:8206`). 30h · L · TD-012
24. **Builder-ize `init_agent`** — `agent/agent_init.py:154` (1,559 lines) into ~10 composed `_init_*` steps. 30h · L · TD-010
25. **Decompose `AIAgent`** — `run_agent.py:320` (4,924 lines/217 methods): extract credential/OAuth refresh, provider quirks, hook sanitization, session persistence. 80h · L · TD-006
26. **Decompose `HermesCLI`** — `cli.py:3192` (10,199 lines): pull worktree lifecycle (:1227-1583, already reverse-imported by two other modules), config load/save (:356, :3145), modal state machine out of `run()` (:10938). 60h · L · TD-007/008
27. **Dispatch-table `resolve_provider_client`** — `agent/auxiliary_client.py:3337` (654 lines, 16 provider branches); the `_try_*` resolvers :1545-2198 are ready building blocks. 20h · L · TD-013
28. **Split `auxiliary_client.py` (5,950 lines) into ~6 modules** — proxy loading :72, header builders :389-503, wrapper class families :640-1127, resolvers :1545-2198, error classification :2295-2560, client cache :4351-4548, call_llm pair. 24h · L · TD-020(agent)
29. **Extract the interruptible-call skeleton** — `chat_completion_helpers.py:125` vs `:1567`: `_close_request_client_once` verbatim ×2 (both carry the #29507 fix), stale detector mirrored by comment, interrupt-join loop ×2; ~150 sharable lines. 12h · M · TD-037
30. **Registry-ize `run_doctor`** — `hermes_cli/doctor.py:465` runs 1,850 lines to EOF; convert inline sections to a check registry (helpers :182-201 already exist). 12h · M · TD-009
31. **MIGRATIONS table for `migrate_config`** — 13 accreted `if current_ver <` gates, 29 `load_config()` calls in the file (`hermes_cli/config.py:4361`). 8h · M · TD-016
32. **Move `cmd_profile` bodies into `profiles.py`** — 13-branch inline dispatcher (`main.py:10003`); logic home (1,817 lines) already exists. 8h · M · TD-022
33. **Extract Telegram topic-bindings + handoff queue from `SessionDB`** — ~430 lines of platform product logic inside the storage layer (`hermes_state.py:4092-4525`, `:4696-4782`). 12h · M · TD-021
34. **4-way split of `kanban_db.py`** — schema/migrations, state machine, boards, notifications (7,750 lines/160 defs). 15h · L · TD-020
35. **Unify error classification** — delete `auxiliary_client.py:2353-2560`'s six `_is_*_error` re-implementations in favor of `agent/error_classifier.py` (the :2418 workaround is already centralized at error_classifier :474). 10h · M · TD-045
36. **De-fork `plugin_llm.py` sync/async trios** — similarity 0.73-0.83 (`complete`/`acomplete` etc.); wrap sync with `asyncio.to_thread` per the in-repo precedent. 8h · M · TD-046
37. **Register Gemini adapters in the transports layer** — `agent/transports/__init__.py:49-68` discovers only 4 transports; gemini ×2 + google_code_assist each re-implement content coercion/response fabrication/stop-reason mapping the layer was built to unify. 24h · L · TD-128
38. **Camofox behind the browser-backend interface** — remove the 13 `if _is_camofox_mode():` forks in browser_tool (:2387-:3688), the bidirectional private imports, and unify session registries so reapers see Camofox tabs (`browser_tool.py:3535-3538` vs `browser_camofox.py:253-363`). 16h · L · TD-041/091
39. **Collapse the four CDP clients onto `CDPSupervisor` with a public dispatch method** — `browser_cdp_tool.py:94-183` and `:190-297` (which pokes supervisor privates), `browser_supervisor.py:465-560`, `browser_tool.py:2841-2944`; also delete the hand-mirrored endpoint resolution in `tui_gateway/server.py:9602-9633`. 16h · M · TD-040
40. **Fix the `browser_dialog` cloud gate** — supervisor runs for Browserbase with `must_respond` default, but the tool is gated on a static CDP override: pending dialogs appear with no tool to answer (`browser_cdp_tool.py:525-552`, `browser_supervisor.py:47`, `browser_tool.py:367-375`). 4h · S · TD-092
41. **Data-driven platform setup wizards** — split-brain between `hermes_cli/gateway.py:5011-5763` (7 wizards) and `hermes_cli/setup.py:1693-2030` (4 wizards, importing qqbot back from gateway.py); ~1,500 near-identical lines → descriptor table. 15h · L · TD-061
42. **One model-selection engine** — four parallel implementations: `main.py:2651`+`model_setup_flows.py`, `model_switch.py:627`, `setup.py:694`, `web_server.py:3243`. 20h · L · TD-038
43. **Shared slash-command executor layer** — implementations ×3-4 (cli.py :7224 + cli_commands_mixin, gateway/slash_commands.py, tui_gateway/slash_worker.py) held in sync by ~35 "Mirrors …" line-number comments that rot (`cli.py:8017`, `run.py:597,5572,9084,12358,14821`). 30h · L · TD-039
44. **Unify the gateway event→UI-state reducer** — `ui-tui/src/app/createGatewayEventHandler.ts` (921 ln) vs `apps/desktop/src/app/session/hooks/use-message-stream.ts` (1,141 ln) + their 1,564/740-line test twins. 60h · L · TD-042
45. **Make `apps/shared` actually shared** — its JSON-RPC client (336 ln) is desktop-only; web (253 ln) and ui-tui (744 ln) re-implement it. First step of the frontend consolidation. 40h · M · TD-042
46. **Split the four giant test files** — `test_tui_gateway_server.py` 7,485, `test_run_agent.py` 6,619, `test_web_server.py` 5,421, `test_feishu.py` 4,946: atomic units under per-file CI sharding, near the 140s kill limit; highest-leverage CI-latency change. 12h · L · TD-106
47. **Burn down raw `sys.modules` mutations in tests** — 178 sites across 82 files force the subprocess-per-file CI architecture; migrate to monkeypatch fixtures to eventually allow plain `pytest tests/`. 20h+ · L · TD-100
48. **Injectable clock/event for text-batching tests** — 37 real sleeps with 50ms margins in `tests/gateway/test_text_batching.py:53-147`; same medicine for `test_run_progress_topics.py` (14 sleeps racing a background task) and `test_stream_consumer.py` (17 sub-0.1s sleeps). The correct pattern already exists in `test_platform_reconnect.py:195-408`. 12h · M · TD-103
49. **Kanban `kanban_common.py`** — de-triplicate `_extract_json_blob` (`kanban_specify.py:111`, `kanban_decompose.py:145`, `profile_describer.py:138`) and `_profile_author` (drifted; copies document the circular-import workaround). 3h · S · TD-053
50. **One skill-identity resolver** — four resolvers, three semantics (`skills_tool.py:599`, `skill_manager_tool.py:278`, `skill_usage.py:789`, `skill_view` inline :1019-1058): a skill whose dir ≠ frontmatter name can be viewed but not edited/deleted. 8h · M · TD-127

## Tier 3 — Solid wins (schedule opportunistically)

51. **De-side-effect `skill_view`** — a "view" call captures secrets, registers env passthrough, mounts credentials (`skills_tool.py:1350-1414`); split the sub-file-serving second endpoint (:1170-1269) out. 12h · L · TD-024
52. **Merge the STT/TTS quoting helpers** — `transcription_tools.py:386/:418` == `tts_tool.py:625/:656` (48 lines). 2h · S · TD-050
53. **Merge `_prepare_anthropic_messages_for_api`/`_prepare_messages_for_non_vision_model`** — `run_agent.py:4462/:4490`; the comment admits identical behavior. 2h · S · TD-051
54. **Point `cli.py` formatters at `usage_pricing`** — `cli.py:105-141` verbatim copies of `agent/usage_pricing.py:874-908` inside an otherwise lazy-import shim block. 2h · S · TD-052
55. **Single `_diagnostic_key`** — `agent/lsp/client.py:911` == `agent/lsp/manager.py:608` (byte-identical; docstring demands they stay identical — so make it one function). 2h · S · TD-055
56. **Shared `ProviderHTTPError` base** — identical error `__init__` in `google_code_assist.py:78` and `gemini_native_adapter.py:158`; also de-fork `gemini_http_error` (0.40 similarity drift, cloudcode +60 lines). 5h · S · TD-056
57. **Shared tool-output pipeline** — truncation notice + ANSI-strip/redact duplicated verbatim between `terminal_tool.py:2420-2437` and `code_execution_tool.py:1413-1446`; JSON error envelope hand-built ×11 in terminal_tool. 5h · S · TD-057
58. **De-duplicate `_save_config_key`** — byte-identical 17-line closure in two methods (`gateway/slash_commands.py:2071/:2278`). 1h · S · TD-058
59. **Move `web/src/lib/fuzzy.ts`==`ui-tui/src/lib/fuzzy.ts` into `apps/shared`**. 2h · S · TD-059
60. **One `_ra()` helper** — identical lazy-import function defined 6× across agent/. 1h · S · TD-060
61. **Merge `_read_skill_name`** — verbatim 20 lines (`skills_sync.py:154-173` == `skill_usage.py:394-413`); also collapse the two `.bundled_manifest` readers and route `.hub/lock.json` parsing through `HubLockFile`. 3h · S · TD-054
62. **One skill-tree scanner** — `skills_tool.py:599-676` vs `skill_commands.py:263-331` (~40 near-identical lines, same gates/dedupe). 4h · S · TD-054
63. **One injection-pattern list for skills** — `skills_tool.py:161-172` substrings vs `skills_guard.py:187-210` regexes covering the same phrases. 3h · S · TD-054
64. **One skill-name validator** — `skill_manager_tool.py:168-178` vs `skills_hub.py:94-119` accept different alphabets; hub installs names manage rejects. 2h · S · TD-054
65. **Adopt `MessageDeduplicator` in the 3 holdout adapters** — feishu :4356, ntfy :367, photon :441 (helpers.py:27 names feishu as its own source). 2h · S · TD-049
66. **Mention-matching into a shared mixin** — 5 near-identical `_compile_mention_patterns` (dingtalk :419, whatsapp_common :168, bluebubbles :163, telegram :5181, photon :256); byte-identical matcher ×3. 5h · M · TD-047
67. **Retire the third batching mechanism** — `base.py:3499-3663` `_text_debounce_*` once TextBatchAggregator is adopted (#1). 6h · M · TD-048
68. **`helpers.guess_extension`** — 4 divergent copies (wecom :821, feishu :3835, signal :78, simplex :104). 2h · S · TD-063
69. **`_BaseTimeoutView` for the Discord views** — 4 near-identical `on_timeout` (discord adapter :5553/:5669/:5765/:6281); move the 849-line `_define_discord_view_classes` (:5447) into a lazily-imported submodule. 5h · M · TD-025
70. **Fix repeated-dict-key bugs** — `hermes_cli/model_normalize.py:63` ("trinity" mapping silently dead), `matrix.py:936-937` (unicode-variant emoji), `scripts/release.py:159/:241`. 1h · S · TD-095
71. **De-duplicate release identity map into `.mailmap`** — `scripts/release.py` (hottest churn, 72 ruff findings) hand-maintains a contributor map duplicating root `.mailmap`. 4h · S · TD-150
72. **Extract worktree lifecycle from `cli.py`** — `_setup_worktree`/`_cleanup_worktree`/`_prune_*` (:1227-1583) reverse-imported by `hermes_cli/main.py:1947` and `tools/delegate_tool.py`. 6h · S · TD-120
73. **Move config load/save out of `cli.py`** — `load_cli_config` (:356, 349 lines) + `save_config_value` (:3145) reverse-imported by hermes_cli while `hermes_cli/config.py` is the nominal home. 8h · M · TD-120
74. **De-cycle `setup.py`/`fallback_cmd.py` from `hermes_cli.main`** — leaf modules import `select_provider_and_model`/`_require_tty` from the 12.5k-line entry point (setup.py:714, fallback_cmd.py:149), which also has import-time side effects (SystemExit at main.py:245-251, sys.argv mutation :505). 7h · M · TD-011
75. **Finish `select_provider_and_model` extraction into `model_setup_flows.py`** — "Phase 2" comment at main.py:584-586; ~1,100 lines of aux-model menu + custom-provider CRUD still in main.py (:3227-3712). 12h · M · TD-038
76. **UI-agnostic cores for auth flows** — web_server re-wraps ~1,100 lines of OAuth because auth.py helpers "print/block/poll" (comment `web_server.py:6042`); falls out of #9 but worth doing first for Anthropic (implemented twice within hermes_cli alone: main.py:4008 vs web_server.py:5405). 12h · M · TD-035
77. **Kill kanban dual dispatch conventions** — `_cmd_*`→int vs `cmd_*`→None (only `main.py:4176` bridges); rationalize `cmd_`/`_cmd_`/`_handle_`/`*_impl` (82/81/37/7 defs). 4h · S · TD-140
78. **Break `build_parser` (kanban) into per-command builders** — 657 lines of pure wiring (`hermes_cli/kanban.py:192`). 6h · M · TD-026
79. **Named functions for MCP server tools** — `mcp_serve.py:450` defines every tool as a nested closure (410 lines), untestable individually. 6h · M · TD-027
80. **Split `web/src/lib/api.ts`** — 2,210-line REST client. 12h · M · TD-028
81. **Type the ui-tui test fixtures** — 202 `any` in ui-tui, 163 of them in `createGatewayEventHandler.test.ts` (events cast `as any`). 10h · M
82. **Resolve the exhaustive-deps suppressions** — 12 in web (`ProfileProvider.tsx:61,78,109`, `ChannelsPage.tsx:207,671`…), 2 in desktop composer (:1240, :1547) — latent stale-closure bugs. 8h · M
83. **Fix wall-clock assertions in tests** — 25+ files assert sub-second ceilings (`test_shutdown_forensics.py:89`, `test_mcp_startup.py:83` incl. a lower bound :139); replace with event-driven sync. 8h · M · TD-103
84. **Chrome port collision in browser tests** — hardcoded 9225 with an xdist offset that is always zero under per-file CI (`test_browser_supervisor.py:28-31`); also decide whether these ever run in CI. 4h · M · TD-104
85. **Nightly lane for `tests/integration/`** — 7 triple-excluded files with no scheduled run; unverifiable rot. 8h · M · TD-105
86. **Migrate 53 legacy `get_event_loop().run_until_complete` tests to `pytest.mark.asyncio`** and delete the autouse loop shim (`tests/conftest.py:452-494`); set `asyncio_mode`. 5h · M · TD-109
87. **Relocate misplaced tests** — `tests/test_web_server.py` (77 ln) beside its 5,421-line sibling; `test_tui_gateway_server.py` at root while `tests/tui_gateway/` exists; 91 root files vs subdirs; rename issue-number tests (`test_35809_*`, `test_860_dedup`…) to behavior names; remove the commented-out skip fossil (`test_413_compression.py:10`). 8h · M · TD-108
88. **Move the RL/datagen pipeline out of the wheel** — `mini_swe_runner.py` (test-only importer), `batch_runner.py`, `trajectory_compressor.py`, `toolset_distributions.py` at repo root and in py-modules (pyproject.toml:283). 6h · S · TD-075
89. **Retire `claw.py`** — 809-line one-time OpenClaw migration (main.py:11427). 2h · S · TD-073
90. **Delete root junk / stale plans** — `hermes-already-has-routines.md` (marketing post), `.plans/` + `plans/` describing shipped features; consolidate on `docs/plans/`. 2h · S · TD-074
91. **Move `plugins/*/dashboard/dist/` source out of `dist/`** — hand-written IIFE misfiled as build output; reconcile the two drifted `plugin_api.py` shims. 3h · S · TD-076
92. **Purge ~45 dead imports/vars** — ruff F401/F811 sweep incl. the noqa'd dead `_canonical_whatsapp_identifier` (`run.py:1384`), matrix crypto imports (:501-504), 9 unused `say` handlers in slack.py. 4h · S · TD-080
93. **Un-vendor or upstream the `ink` fork strategy decision** — 68k-line fork incl. hand-ported yoga (`ui-tui/packages/hermes-ink/`); at minimum stop double-shipping real `ink` alongside the fork via the `ink-text-input` peer (`entry-exports.ts:32`). 8h (decision + dedup) · M · TD-078
94. **Split runtime-deps root from npm workspace root** — Python-only `hermes update` users currently pull a 672 KB lock covering desktop/web/tui (root `package.json` dual purpose; 4 lockfiles/3 islands). 10h · M · TD-125
95. **Rename `hermes_constants.py`** (568 lines of platform/path functions, ~30 defs, almost no constants) and de-duplicate `get_config_path`/`get_env_path` with `hermes_cli/config.py:600-604`. 3h · S · TD-129
96. **Adapter naming pass** — `*_adapter` modules exporting `*Client` classes, `AnthropicTransport` third vocabulary, `azure_identity_adapter` misnomer, `_diag_key` vs `_diagnostic_key`. 4h · M · TD-141
97. **Skill-taxonomy reconciliation** — 9 category dirs exist in both `skills/` and `optional-skills/` with different membership; document or merge. 6h · M · TD-126
98. **Config-surface generation** — generate `cli-config.yaml.example` (64 KB/112 keys) and `.env.example` (23 KB) from code or a schema instead of triple hand-sync with gateway/config.py's 198 env vars. 12h · M · TD-151
99. **Migrate remaining builtins to the platform registry** — end the dual loading regime (legacy if/elif `_create_adapter` run.py:6404 vs `ctx.register_platform`); discord proved the plugin path. 8h+ · L · TD-122
100. **i18n sync tooling** — five hand-synced systems (locales/ 16 langs, desktop 4, web 17, website zh-Hans 318 files, translated READMEs already drifted); at minimum a CI check that keys/locales match per surface. 12h (tooling) · M · TD-123

---
**Not ranked but noted:** `yuanbao` platform (~8k lines, single-drop, zero follow-up commits — assign an owner or schedule removal review, TD-130); `apps/bootstrap-installer` cross-package CSS import (TD-131); lodash exact-pin without rationale (TD-154); `AGENTS.md`/`CONTRIBUTING.md` 114 KB combined hand-maintained guidance (TD-152).
